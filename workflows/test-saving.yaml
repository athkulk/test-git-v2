# {"Notebook-task":{"x":390,"y":223}}
test-saving:
  dag_id: test-saving
  tasks:
    Notebook-task:
      operator: >-
        airflow.providers.amazon.aws.operators.sagemaker_unified_studio.SageMakerNotebookOperator
      input_config:
        input_params: {}
        input_path: getting_started.ipynb
      compute: {}
      output_config:
        output_formats:
          - NOTEBOOK
