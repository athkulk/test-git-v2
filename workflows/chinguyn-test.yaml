# {"Notebook-task":{"x":231,"y":250}}
chinguyn-test:
  dag_id: chinguyn-test
  tasks:
    Notebook-task:
      operator: >-
        airflow.providers.amazon.aws.operators.sagemaker_unified_studio.SageMakerNotebookOperator
      input_config:
        input_params: {}
        input_path: getting_started.ipynb
      compute: {}
      output_config:
        output_formats:
          - NOTEBOOK
